name: Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Typecheck & build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npx tsc -b

      - name: Build
        run: npm run build
        env:
          # All configured as repository Secrets (not Variables) - vars.*
          # silently resolves to an empty string for a name that only
          # exists as a secret, so these must match secrets.* exactly.
          VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL }}
          VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 1

  deploy:
    name: Deploy to Namecheap (FTPS)
    needs: build
    runs-on: ubuntu-latest
    env:
      FTP_SERVER: ${{ secrets.FTP_HOST || secrets.SFTP_HOST || 'ftp.dripemporium.store' }}
      FTP_PORT: ${{ secrets.FTP_PORT || secrets.SFTP_PORT || '21' }}
      FTP_USERNAME: ${{ secrets.FTP_USERNAME || secrets.SFTP_USERNAME }}
      FTP_PASSWORD: ${{ secrets.FTP_PASSWORD || secrets.SFTP_PASSWORD }}
      FTP_REMOTE_PATH: ${{ secrets.FTP_REMOTE_PATH || secrets.SFTP_REMOTE_PATH }}
    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Validate FTPS configuration
        run: |
          test -n "$FTP_SERVER" || { echo "::error::Missing FTP server"; exit 1; }
          test -n "$FTP_USERNAME" || { echo "::error::Missing FTP_USERNAME or SFTP_USERNAME secret"; exit 1; }
          test -n "$FTP_PASSWORD" || { echo "::error::Missing FTP_PASSWORD or SFTP_PASSWORD secret"; exit 1; }
          test -n "$FTP_REMOTE_PATH" || { echo "::error::Missing FTP_REMOTE_PATH or SFTP_REMOTE_PATH secret"; exit 1; }

      - name: Deploy over FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          # Keep the deploy job compatible with both the newer FTP_* secret
          # names and the older SFTP_* ones already present in the repo.
          server: ${{ env.FTP_SERVER }}
          port: ${{ env.FTP_PORT }}
          username: ${{ env.FTP_USERNAME }}
          password: ${{ env.FTP_PASSWORD }}
          protocol: ftps
          local-dir: dist/
          server-dir: ${{ env.FTP_REMOTE_PATH }}
